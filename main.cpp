#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    // int wnum = 5;
    // double dnum = 5.5;

    // wnum++;
    // cout << wnum;

    // cout << pow(2, 10);

    cout << round(36) << endl;
    cout << fmax(3, 10) << endl;
    return 0;
}